apiVersion: v2
name: kubecost-wrapper
version: 1.0.0
description: A wrapper chart for kubecost
dependencies:
  - name: cost-analyzer
    version: "2.8.0"
    repository: "https://kubecost.github.io/cost-analyzer/"