name: backend

replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx

  host: api.local
  path: /

  annotations: {}

  tls: false
  tlsSecret: ""

resources: {}