apiVersion: v1
kind: ConfigMap
metadata:
  name: stagelog-observability-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  stagelog-observability.json: |-
{{ .Files.Get "dashboards/stagelog-observability.json" | indent 4 }}