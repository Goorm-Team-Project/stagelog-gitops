apiVersion: argoproj.io/v1alpha1
kind: AppProject

metadata:
  name: dev-stagelog
  namespace: argocd

spec:
  description: Dev environment project

  sourceRepos:
    - https://github.com/org/stagelog-gitops

  destinations:
    - namespace: dev-*
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

    